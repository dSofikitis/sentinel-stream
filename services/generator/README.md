# sentinel-generator

Replays a security-event corpus (or generates synthetic events) and
posts them at a configurable rate to the `ingest` service. Used to
drive the pipeline during demos and load tests.

Implementation lands in phase 3. The current commit only ships a stub
to keep CI green.
