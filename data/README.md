# data

Seed corpora for the `generator` service to replay during demos and
load tests. Will hold synthetic auth / network / DNS event JSONL
files. Real-world Sigma test data is fetched at build time, not
checked in.
