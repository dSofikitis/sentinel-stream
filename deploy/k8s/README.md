# deploy/k8s

Reserved for Kubernetes manifests / Helm chart. SentinelStream
currently ships with the Compose stack in `../compose/`; the topic
contracts, schemas, and service boundaries are designed so a
Kubernetes port is a mechanical translation rather than a redesign.
